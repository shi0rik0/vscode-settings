{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug current file with args",
            "type": "debugpy",
            "request": "launch",
            "program": "${file}",
            "console": "integratedTerminal",
            "args": ["${command:pickArgs}"],
            "justMyCode": false
        }
    ]
}
